# Security

The canonical security document is
[`security/security-overview.md`](security/security-overview.md).

This compatibility entry point is retained so existing repository links do not
break. The canonical document contains the migrated secret, Keychain,
SwiftData, SSH host-key, FTPS, FTP, RDP, logging, import/export, signing,
hardening, assumptions, risks, and follow-up sections.
