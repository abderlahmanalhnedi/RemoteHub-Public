# Architecture

The canonical architecture document is
[`architecture/system-overview.md`](architecture/system-overview.md).

This compatibility entry point is retained so existing repository links do not
break. The canonical document contains the migrated layer description,
sensitive connection and credential flows, persistence boundaries, adapter
design, concurrency model, external dependencies, Mermaid diagrams, and the
Xcode/Swift Package build shape.
